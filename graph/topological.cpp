#ifndef TOPOLOGICAL
#define TOPOLOGICAL

std::vector<int> topological_sort(std::vector<int> links[]){
	int n = sizeof(links) / sizeof(std::vector<int>);
	printf("%ld\n", sizeof(links));
	return std::vector<int>();
}


#endif
